import { Component } from '@angular/core';

@Component({
  selector: 'app-player', // the selector you'll use in your HTML
  templateUrl: './player.component.html', // the HTML template file
  styleUrls: ['./player.component.scss'] // the style file
})
export class PlayerComponent {}
